{
  "name": "az-express-rules-tests",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore \"node --test rules-tests/firestore.rules.test.js\"",
    "test:storage": "firebase emulators:exec --only storage \"node --test rules-tests/storage.rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1"
  }
}

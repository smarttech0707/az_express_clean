{
  "name": "az-express-rules-tests",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore \"node --test rules-tests/*.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1"
  }
}
